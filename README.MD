### Using floodfill to make the background / border transparent

WIP!

python deps to install
```
pip install numpy
sudo pip install pillow
brew tap homebrew/science
brew install opencv3 --with-contrib
```